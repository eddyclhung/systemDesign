name: Deploy cheat sheets to GitHub Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build GitHub-compatible HTML + Markdown
        run: |
          python3 scripts/build_prerendered_html.py --pages-deploy
          python3 scripts/build_github_view.py
          python3 -c "import re,sys; t=open('cheatSheet/system_design_cheatsheet_v14.html').read(); n=len(re.findall(r'id=\"card-\d+\"', t)); print(f'v14 card check: {n}'); sys.exit(0 if n==40 else 1)"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: cheatSheet

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
